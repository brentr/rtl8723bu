Simply run:

$ ltibMake

To build rtl8723bu.ko, then:

$ arm-none-linux-gnueabi-strip --strip-unneeded rtl8723bu.ko

Beware that the above strip command
leaves about 64KB of strange symbols in the resulting object file
when run on 64-bit system.  These extra symbols are stripped 
properly when run on a 32-bit Linux system.

.ko file should be 866208 bytes, not >900kB

$ cp rtl8723bu.ko ~/ltib/rootfs.tmp/lib/modules/3.18.140-lpc31-mbari4/kernel/drivers/net/wireless

11/17/22 brent@mbari.org
 
