Simply run:

$ ltibMake

To build rtl8723bu.ko, then:

$ arm-none-linux-gnueabi-strip --strip-unneeded rtl8723bu.ko

To install the kernel object file in the image:
(note that the required firmware is embedded into the kernel object)

$ cp rtl8723bu.ko ~/ltib/rootfs.tmp/lib/modules/3.18.140-lpc31-mbari4/kernel/drivers/net/wireless

11/17/22 brent@mbari.org
 
